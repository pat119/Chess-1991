package com.example.myapplication.pieces;

public class PowerUp{
    String imageSource;

    // Tells what kind of power up from an internal list.
    private int type;

    // We might want power ups to fade after a few turns, and if so we should track how many turns
    // left.
    private int life;
}
